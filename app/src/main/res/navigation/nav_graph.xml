<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/postsFragment">

    <fragment
        tools:layout="@layout/posts_fragment"
        android:id="@+id/postsFragment"
        android:name="com.github.krottv.tmstemp.view.PostsFragment"
        android:label="Список пользователей">
        <action
            android:id="@+id/action_postsFragment_to_fullscreenFragment"
            app:destination="@id/fullscreenFragment" />
    </fragment>
    <fragment
        tools:layout="@layout/posts_fragment"
        android:id="@+id/fullscreenFragment"
        android:name="com.github.krottv.tmstemp.view.FullscreenFragment"
        android:label="{title}">

        <argument
            android:name="title"
            app:argType="string"/>

        <argument
            android:name="post"
            app:argType="com.github.krottv.tmstemp.domain.PostModel" />
    </fragment>
</navigation>